{
  "name": "motionview",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "vendor:sync": "node scripts/sync_vendor_assets.js",
    "py:build": "node scripts/build_py.js",
    "dev": "pnpm vendor:sync && pnpm py:build && tauri dev",
    "build": "pnpm vendor:sync && pnpm py:build && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "dotenv-cli": "^11.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "chart.js": "^4.5.1"
  }
}
